# Load explicitly using uvicorn --env-file .env from the repository root.
# Generate a random token (at least 32 characters); never use a vCenter password here.
VCF_STUDIO_API_TOKEN=
VCF_STUDIO_VCENTER_HOSTS=vc.lab.example
VCF_STUDIO_ALLOW_INSECURE_TLS=false
